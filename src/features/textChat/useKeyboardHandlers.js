export function createKeyboardHandlers(handleSendMessage) {
  const handleKeyDown = (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSendMessage();
    }
  };

  return { handleKeyDown };
}
